language: python

python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"

before_install:
  - sudo apt-get install -qq python-scipy python3-scipy
  - pip install Cython

install:
  - python setup.py install

script:
  - cd tests && python test.py
